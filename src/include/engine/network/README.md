# Engine Network Headers

Private headers for modern network buffer, channel, socket, and protocol
helpers.

Current private headers:

- `network_buffer.hpp`: modern bit-buffer primitive used behind selected
  legacy `MSG_*` helpers.
