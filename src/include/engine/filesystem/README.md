# Engine Filesystem Bridge Headers

Reserved for private engine-side filesystem bridge headers.

Current headers:

- `mount_flags.hpp`: C++ mount-content selection and bit construction helper.
- `mount_flags_adapter.h`: C-compatible adapter and constants for the legacy
  engine filesystem bridge.
