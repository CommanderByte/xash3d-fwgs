# Modern Engine Private Headers

Private headers for modern engine internals live here.

These headers are not public SDK or DLL ABI headers. Keep public C-compatible
engine contracts in their current legacy locations until a specific migration
phase proves a safe replacement.

Subfolders mirror `src/engine/` implementation lanes.
