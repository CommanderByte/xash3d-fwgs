# xash3d-fwgs repo venv (.venv, Python 3.14) — reproduce with:
#   .venv\Scripts\python.exe -m pip install -r requirements.txt
#
# Markdown lint/format toolchain (pre-existing):
application_file_scanner==0.6.4
application_properties==0.9.2
Columnar==1.4.1
markdown-it-py==4.2.0
mdformat==1.0.0
mdurl==0.1.2
py-walk==0.3.3
pyjson5==2.0.1
pymarkdownlnt==0.9.37
PyYAML==6.0.3
sly==0.5
tomli==2.4.1
toolz==1.1.0
typing_extensions==4.15.0
wcwidth==0.7.0
# Agent tooling (xash3dpp/tools): the CLI scripts are stdlib-only; the `mcp`
# package is needed ONLY by xash3dpp/tools/mcp_server.py (FastMCP).
mcp>=1.2
