# Codex Artifacts

This folder holds small archival artifacts from the modular rewrite branch that
are useful for future reference but should not be expanded into the clean
restart branch.

- `codex-cache-20260514.zip` preserves the local `.codex-cache/` reports and
  local-agent notes that were present when the master-based restart branch was
  created.
