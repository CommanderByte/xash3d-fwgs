# xash3dpp — networking subsystem
# Socket I/O, Netchan reliability/fragmentation, HTTP, master-server.
# Legacy reference: engine/common/net_ws.c, net_chan.c, net_buffer.c,
#                   net_encode.c, masterlist.c, http/net_http_xash.c
#
# TODO: add source files and add_library target when implementation begins.
# Likely dependencies: xash3dpp_utilities, xash3dpp_platform
