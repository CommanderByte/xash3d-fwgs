# xash3dpp — world subsystem
# BSP spatial queries, entity linking, collision/trace, PVS/PAS.
# Legacy reference: engine/server/sv_world.c, engine/common/world.c,
#                   engine/common/pm_trace.c, pm_surface.c
#
# TODO: add source files and add_library target when implementation begins.
# Likely dependencies: xash3dpp_content, xash3dpp_utilities
