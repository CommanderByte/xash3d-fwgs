# xash3dpp — platform subsystem
# OS abstraction: time, sleep, DLL load, dialogs, clipboard, window management.
# Legacy reference: engine/platform/  (win32/, posix/, sdl2/, android/, ios/)
#
# TODO: add source files and add_library target when implementation begins.
# Likely dependencies: xash3dpp_utilities
